import { createSlice } from "@reduxjs/toolkit"
const initialState ={
  
}
export const AppSlice
 = createSlice({
            name:"Store",
            initialState,
            reducers:{
               
            }
})



export default AppSlice.reducer